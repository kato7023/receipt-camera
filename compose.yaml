name: receipt-camera

services:
  app:
    build: .
    image: receipt-camera-app:latest
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    tty: true

volumes:
  node_modules:
